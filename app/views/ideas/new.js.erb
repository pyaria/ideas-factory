// $('.modal').html("<%= new_javascript(render(new)) %>"); //inserts content into your empty div, be aware that the parameter needed to be quoted.
// $('.modal').dialog(); //jquery ui will open it as a modal popup
